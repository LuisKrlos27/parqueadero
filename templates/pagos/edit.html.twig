{% extends 'base.html.twig' %}

{% block title %}Edit Pagos{% endblock %}

{% block body %}
    <h1>Edit Pagos</h1>

    {{ include('pagos/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_pagos_index') }}">back to list</a>

    {{ include('pagos/_delete_form.html.twig') }}
{% endblock %}
