{% extends 'base.html.twig' %}

{% block title %}TiposVehiculos{% endblock %}

{% block body %}
    <h1>TiposVehiculos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipos_vehiculo.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tipos_vehiculo.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tipos_vehiculos_index') }}">back to list</a>

    <a href="{{ path('app_tipos_vehiculos_edit', {'id': tipos_vehiculo.id}) }}">edit</a>

    {{ include('tipos_vehiculos/_delete_form.html.twig') }}
{% endblock %}
